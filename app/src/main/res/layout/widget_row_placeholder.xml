<?xml version="1.0" encoding="utf-8"?>
<!--
  What the collection factory returns for a row index with no data, which the framework can ask for
  between a data-set change and the count catching up. Zero height so it never shows.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="0dp" />
