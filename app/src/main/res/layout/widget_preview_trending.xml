<?xml version="1.0" encoding="utf-8"?>
<!--
  Picker preview for Trending.

  Static XML with sample content, mirroring widget_trending.xml. It reuses the real drawables and
  @color/widget_* resources rather than a frozen preview palette, so it follows light and dark and,
  on API 31+, the system colour.

  It cannot follow the user's chosen seed the way the live widget does: the picker inflates this
  without ever running our code, so there is nothing to tint it with.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_bg"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingHorizontal="16dp">

        <FrameLayout
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="@drawable/widget_icon_plate_bg">

            <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_gravity="center"
                android:contentDescription="@null"
                android:src="@drawable/local_fire_department_24px"
                android:tint="@color/widget_on_primary" />
        </FrameLayout>

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_weight="1"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="@string/widget_trending_title"
            android:textColor="@color/widget_on_surface"
            android:textSize="18sp"
            android:textStyle="bold" />

        <FrameLayout
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginStart="8dp"
            android:background="@drawable/widget_icon_plate_muted_bg">

            <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_gravity="center"
                android:contentDescription="@null"
                android:src="@drawable/ic_search_24px"
                android:tint="@color/widget_on_surface_variant" />
        </FrameLayout>
    </LinearLayout>

    <include layout="@layout/widget_preview_row_ranked" />

    <include layout="@layout/widget_preview_row_ranked" />
</LinearLayout>
