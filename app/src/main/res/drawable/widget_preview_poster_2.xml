<?xml version="1.0" encoding="utf-8"?>
<!-- Second stand-in cover, so a preview list does not look like the same show repeated. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <corners android:radius="8dp" />
    <gradient
        android:angle="45"
        android:endColor="@color/widget_secondary_container"
        android:startColor="@color/widget_primary_container"
        android:type="linear" />
</shape>
