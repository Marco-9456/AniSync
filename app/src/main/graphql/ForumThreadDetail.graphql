query GetForumThread($id: Int!) {
  Thread(id: $id) {
    id
    title
    body
    replyCount
    viewCount
    likeCount
    isLiked
    isSubscribed
    isLocked
    isSticky
    repliedAt
    createdAt
    updatedAt
    siteUrl
    user {
      id
      name
      avatar {
        large
      }
    }
    replyUser {
      name
      avatar {
        large
      }
    }
    categories {
      id
      name
    }
    mediaCategories {
      title {
        romaji
      }
      coverImage {
        medium
      }
    }
  }
}

query GetThreadComments(
  $threadId: Int!,
  $page: Int = 1,
  $perPage: Int = 25,
  $sort: [ThreadCommentSort] = [ID]
) {
  Page(page: $page, perPage: $perPage) {
    threadComments(threadId: $threadId, sort: $sort) {
      id
      comment
      likeCount
      isLiked
      createdAt
      updatedAt
      siteUrl
      childComments
      user {
        id
        name
        avatar {
          large
        }
      }
    }
    pageInfo {
      hasNextPage
      currentPage
    }
  }
}
