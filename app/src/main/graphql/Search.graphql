query SearchMedia(
  $search: String, 
  $type: MediaType, 
  $page: Int, 
  $perPage: Int,
  $status: MediaStatus,
  $format: MediaFormat,
  $genres: [String],
  $seasonYear: Int,
  $season: MediaSeason
) {
  Page(page: $page, perPage: $perPage) {
    media(
      search: $search, 
      sort: POPULARITY_DESC, 
      type: $type,
      status: $status,
      format: $format,
      genre_in: $genres,
      seasonYear: $seasonYear,
      season: $season
    ) {
      id
      title {
        userPreferred
        romaji
        english
        native
      }

      coverImage {
        medium
        large
        extraLarge
      }
      status
      averageScore
      episodes
      chapters
      volumes
      type
      format
      genres
    }
  }
}
