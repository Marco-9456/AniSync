query GetForumThreads(
  $page: Int = 1,
  $perPage: Int = 20,
  $categoryId: Int,
  $search: String,
  $sort: [ThreadSort] = [REPLIED_AT_DESC]
) {
  Page(page: $page, perPage: $perPage) {
    threads(categoryId: $categoryId, search: $search, sort: $sort) {
      id
      title
      replyCount
      viewCount
      likeCount
      isLiked
      isLocked
      repliedAt
      createdAt
      user {
        id
        name
        avatar {
          large
        }
      }
      categories {
        id
        name
      }
    }
    pageInfo {
      hasNextPage
      currentPage
    }
  }
}
